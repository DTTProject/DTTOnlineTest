<!DOCTYPE html>
<html>
  <%= render "partials/head" %>
  <body class="fixed-sn mdb-skin admin_panel">
    <%= render "shared/message"%>
    <!-- Message toast -->
    <!--Double navigation-->
    <%= render "admin/partials/header" %>
    <!--/Double navigation-->

    <!--Main layout-->
    <main class="admin_content">
      <div class="container-fluid">
      <div class="row">
        <div class="col-md-10 offset-md-2">
          <div class="header_title">
            <h3><%= yield :admin_header_title %></h3>
          </div>
          <hr/>
          <div class="content_detail">
            <%= yield %>
          </div>
        </div>
      </div>
      </div>
    </main>
    <!--/Main layout-->
    <%= render "admin/partials/footer" %>
    <%= render "partials/modal_login" %>
    <script>
      var quill = new Quill('#editor-container', {
    modules: {
      toolbar: [
        ['bold', 'italic'],
        ['link', 'blockquote', 'code-block', 'image'],
        [{ list: 'ordered' }, { list: 'bullet' }]
      ]
    },
    placeholder: 'Write content here...',
    theme: 'snow'
  });

  var quill1 = new Quill('#editor-container-1', {
    modules: {
      toolbar: [
        ['bold', 'italic'],
        ['link', 'blockquote', 'code-block', 'image'],
        [{ list: 'ordered' }, { list: 'bullet' }]
      ]
    },
    placeholder: 'Suggestion for question ...',
    theme: 'snow'
  });
    </script>
  </body>
</html>


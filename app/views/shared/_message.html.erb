<% unless flash.empty? %>
    <script type="text/javascript">
        <% flash.each do |f| %>
          Materialize.toast('<%= f[1] %>', 2000, '<%= f[0].to_s %>');
        <% end %>
    </script>
<% end %>
